<?php

function test_block_theme($existing, $type, $theme, $path) {
  return [
    'default_block' => [
      'variables' => ['hello_var' => NULL],
      'path' => drupal_get_path('module', 'test_block') . '/templates',
    ],
  ];
}
